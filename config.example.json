{
  "_comment": "Copy this file to config.json and fill in your values. config.json is gitignored and will never be committed.",

  "score_threshold": 7,
  "poll_interval_seconds": 21600,
  "max_emails_per_check": 20,

  "alert_channels": {
    "opportunities": {
      "label": "Important Email",
      "mode": "immediate",
      "min_score": 7,
      "description": "Actual people, customers, friends, partners, prospects, school/family logistics, paid work, and direct asks you cannot afford to miss."
    },
    "money_admin": {
      "label": "Money/Admin",
      "mode": "digest",
      "min_score": 9,
      "description": "Bills, receipts, refunds, affiliate payouts, bank notices, and account admin. Usually not immediate."
    },
    "learning_events": {
      "label": "Learning/Events",
      "mode": "digest",
      "min_score": 8,
      "description": "Newsletters, webinars, launches, and event blasts. Useful later, not urgent."
    },
    "sales_pitches": {
      "label": "Sales Pitches",
      "mode": "mute",
      "min_score": 10,
      "description": "Cold outreach and people selling you something."
    },
    "muted": {
      "label": "Muted",
      "mode": "mute",
      "min_score": 10,
      "description": "Low-value noise."
    }
  },

  "model_provider": {
    "url": "http://localhost:1234",
    "model": "qwen2.5-3b-instruct",
    "api_key_env": "",
    "_provider_note": "Any OpenAI-compatible /v1/chat/completions endpoint. Default is LM Studio for local open-source models. For frontier models, point url/model at your provider or proxy and set api_key_env to the environment variable containing the API key."
  },

  "telegram": {
    "bot_token": "YOUR_BOT_TOKEN_HERE",
    "chat_id": "YOUR_CHAT_ID_HERE",
    "feedback_buttons": true,
    "_help": "Bot token from @BotFather. Chat ID from @userinfobot."
  },

  "accounts": [
    {
      "label": "Main",
      "credentials_path": "credentials.json",
      "token_path": "token_main.pkl"
    },
    {
      "label": "Work",
      "credentials_path": "credentials.json",
      "token_path": "token_work.pkl"
    },
    {
      "label": "Media",
      "credentials_path": "credentials.json",
      "token_path": "token_media.pkl"
    }
  ]
}
